import { StyleSheet } from 'react-native';
const styles = StyleSheet.create({
    box: {
        display: 'flex',
    },
    rowcontainer: {
        flexDirection: 'row',
        alignItems: 'center',
    },
});

export default styles;
