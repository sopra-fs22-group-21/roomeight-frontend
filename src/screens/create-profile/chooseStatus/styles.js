import { StyleSheet } from 'react-native';
const styles = StyleSheet.create({
    inner: {
        flex: 1,
        paddingLeft: 20,
        paddingTop: 20,
        paddingRight: 20,
    },
});

export default styles;
