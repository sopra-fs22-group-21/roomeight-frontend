import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    icon: {
        paddingRight: 10,
    },
    row: {
        alignItems: 'center',
        flexDirection: 'row',
    },
});
export default styles;
